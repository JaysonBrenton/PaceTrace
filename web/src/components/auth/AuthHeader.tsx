export function AuthHeader() {
  return (
    <header className="flex w-full items-center justify-center py-10">
      <a
        href="/"
        className="text-center text-xl font-semibold tracking-tight text-foreground transition-colors hover:text-accent"
      >
        PaceTrace — the one-stop lap logic shop.
      </a>
    </header>
  );
}
